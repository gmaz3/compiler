program countDigits
{
    declare x, count;
    declare a, b, c, d, e, f;
    
    # main #
    input(x);
    while (x>0)
    {
        x := a * b + c * d - e * f;
        count := count+1
    };
    print(count);
}.